import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
`;

const Home: React.FC = () => {
  return <Wrapper>Basic App</Wrapper>;
};

export default Home;
